@model AppRole

@{
    ViewData["Title"] = $"{Model.NormalizedName} Role Details";
}

<h1>@ViewData["Title"]</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Db Name @* @Html.DisplayNameFor(model => model.Name) *@
        </dt>
        <dd class="col-sm-10">
            @Model.Name @* @Html.DisplayFor(model => model.Name) *@
        </dd>
        <dt class="col-sm-2">
            Is Active @* @Html.DisplayNameFor(model => model.IsActive) *@
        </dt>
        <dd class="col-sm-10">
            @Model.IsActive @Html.DisplayFor(model => model.IsActive) 
        </dd>
        <dt class="col-sm-2">
            Create Date @* @Html.DisplayNameFor(model => model.CreateDate) *@
        </dt>
        <dd class="col-sm-10">
            @Model.CreateDate.ToString("dd/MM/yyyy HH:mm") @* @Html.DisplayFor(model => model.CreateDate) *@
        </dd>
        <dt class="col-sm-2">
            Last Update Date @* @Html.DisplayNameFor(model => model.UpdateDate) *@
        </dt>
        <dd class="col-sm-10">
            @Model.UpdateDate.ToString("dd/MM/yyyy HH:mm") @* @Html.DisplayFor(model => model.UpdateDate) *@
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-outline-primary">Edit</a>
    <a asp-action="Index" class="btn btn-outline-primary">Back to List</a>
</div>
